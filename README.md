## 📋 Guia de Instalação:

### Pré-requisitos
- [Node.js](httpss://nodejs.org/)
- [NPM](httpss://www.npmjs.com/)

### 1. Em um terminal, clone o repositório:

```bash
 git clone https://github.com/EmmanuelJYokoyama/ATV3_ATLANTIS.git
 cd ATV3_ATLANTIS
 ```
  
### 2. Instale as dependências:

```bash
 npm install
 ```

### 3. Compile e execute a aplicação:

Para compilar o código TypeScript, execute:
```bash
 tsc
 ```
Isso irá gerar os arquivos JavaScript em um diretório `out`.

Para executar a aplicação, use o seguinte comando:
```bash
 node out/app.js
 ```
